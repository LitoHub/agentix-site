{
  "name": "agentix-site",
  "version": "1.0.0",
  "private": true,
  "description": "Landing de Agentix + agenda de demos con calendario nativo integrado a Google Calendar.",
  "scripts": {
    "dev": "netlify dev",
    "start": "netlify dev"
  },
  "dependencies": {
    "googleapis": "^144.0.0",
    "nodemailer": "^6.9.16"
  },
  "devDependencies": {
    "netlify-cli": "^17.0.0"
  }
}
